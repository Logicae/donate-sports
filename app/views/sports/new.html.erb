<div>
<br>
<% if current_user %>
    <ul><h2>Add a product for donation</h2>
    <%= form_for @sport do |f| %></ul>
    
    <ul><strong><%= f.label :name %></strong>
    (choose from existing or add new sport)</ul>
    <ul><%= f.collection_select :id, Sport.all, :id, :name %>
    <%= f.text_field :name %></ul>
    <ul><%= f.submit "Submit" %></ul>
    <% end %>
    <% end %>
</div>
<!-- put in partial -->