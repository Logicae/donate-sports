<br>
    <% if logged_in? %>

        <ul><h2>See All <%= link_to @sport.name, sport_products_path(@sport) %> Listings</h2></ul>
        <ul><h2><%= link_to "Donate", new_sport_product_path(@sport) %> New <%= @sport.name %> Item </h2></ul>

    <% else %>
            <ul>Please sign in to see additional details.</ul>
    <% end %>

<button id="products">Show All Products</button>
<div id="product"></div>

<br>

<button class="js-next" id="<%= @sport.id %>">Next Sport</button>
<h1 class="sportName"><%= @sport.name %></h1>
<h1 class="productName"><%= @sport.name %></h1>


  